{
  "name": "courier-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix vie install",
    "dev": "npm --prefix vie run dev",
    "build": "npm --prefix vie run build",
    "preview": "npm --prefix vie run preview",
    "frontend:install": "npm --prefix vie install",
    "admin:install": "npm --prefix admin-spa install",
    "admin:dev": "npm --prefix admin-spa run dev",
    "admin:build": "npm --prefix admin-spa run build"
  }
}
